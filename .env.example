# =============================================================================
# FERRET - Federal Expenditure Review and Risk Evaluation Tool
# =============================================================================

# -----------------------------------------------------------------------------
# ANTHROPIC API KEY
# -----------------------------------------------------------------------------
# Only required if running as standalone Python script.
# NOT needed if running through Claude Code CLI (uses existing auth).
#
# Get your key: https://console.anthropic.com/
#
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# SAM.GOV API KEY (Optional)
# -----------------------------------------------------------------------------
# Increases rate limit from 10/min to 1000/day.
# The agent works without this, but may hit rate limits on large scans.
#
# How to get:
# 1. Go to https://sam.gov/ and sign in (or create Login.gov account)
# 2. Navigate to Profile → API Keys
# 3. Request "Entity Management" API key
#
SAM_GOV_API_KEY=

# -----------------------------------------------------------------------------
# USASPENDING.GOV
# -----------------------------------------------------------------------------
# No API key required - completely open API
# Docs: https://api.usaspending.gov/
